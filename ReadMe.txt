The requirements are shown as follow:
    - python == 3.6.7
    - pandas == 1.0.3
    - numpy == 1.18.1
    - scikit-learn == 0.23.2
    - xgboost == 0.90

Method to reproduce the result:
    Just open the jupyter notebook and run the code cell from top to bottom, then the result files can be generated.
